{%- comment -%}
  Stub template. The home page is fully built; this page is scaffolding so the
  theme validates and uploads. Replace the body with real sections as each
  design component is converted.
{%- endcomment -%}
<section style="padding:calc(var(--header-h) + var(--banner-h) + var(--space-8)) var(--container-pad) var(--space-9);">
  <h1 class="sv-section-title">Page not found</h1>
  <p style="font-size:15px;color:var(--text-muted);margin-top:var(--space-5);">The page you were looking for is no longer here.</p>
  <a class="sv-btn-outline" href="{{ routes.root_url }}" style="margin-top:var(--space-6);">Back to home</a>
</section>
